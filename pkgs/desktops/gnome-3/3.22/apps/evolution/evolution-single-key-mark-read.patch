diff --git a/mail/e-mail-reader.c b/mail/e-mail-reader.c
index 33e49c9..84545e6 100644
--- a/mail/e-mail-reader.c
+++ b/mail/e-mail-reader.c
@@ -2534,6 +2534,7 @@ mail_reader_key_press_event_cb (EMailReader *reader,
 {
 	GtkAction *action;
 	const gchar *action_name;
+	gboolean shift_pressed;
 
 	if (!gtk_widget_has_focus (GTK_WIDGET (reader))) {
 		WebKitWebFrame *frame;
@@ -2569,6 +2570,8 @@ mail_reader_key_press_event_cb (EMailReader *reader,
 	if ((event->state & GDK_CONTROL_MASK) != 0)
 		goto ctrl;
 
+	shift_pressed = (event->state & GDK_SHIFT_MASK) != 0;
+
 	/* <keyval> alone */
 	switch (event->keyval) {
 		case GDK_KEY_Delete:
@@ -2595,6 +2598,11 @@ mail_reader_key_press_event_cb (EMailReader *reader,
 			action_name = "mail-previous-unread";
 			break;
 
+		case GDK_KEY_K:
+		case GDK_KEY_k:
+			action_name = (shift_pressed) ? "mail-mark-unread" : "mail-mark-read";
+			break;
+
 #ifdef HAVE_XFREE
 		case XF86XK_Reply:
 			action_name = "mail-reply-all";
